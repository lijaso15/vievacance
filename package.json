{
  "name": "fullstack_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": " cd backend && node server.js",
    "build": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build && cd ../backend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm": "^6.10.1"
  }
}
